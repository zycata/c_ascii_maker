# C ascii maker

Simple project that can take an input image and generator ascii-art from the image

Usage:
'''
./ascii.exe path/to/image.png
'''


To build: 
```bash
# Regular build
make

# compiled with optimizations
make release
```

### Optional flags
`-mw` maximum width  (Default terminal height)
`-mh` maximum height (Default terminal height)
